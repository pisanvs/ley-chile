# Copy & SEO — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update all copy across the ley·chile web app to reflect the real mission, add per-page SEO meta/OG/JSON-LD, and ship robots.txt, sitemap.xml, and llms.txt.

**Architecture:** React 19's native document metadata API hoists `<title>` and `<meta>` elements rendered inside any component to `<head>` automatically — no react-helmet needed. JSON-LD structured data is injected via `useEffect` since React 19 does not hoist `<script>` tags. The sitemap is generated by a new Python script at build time from `catalog.json`. Static files (robots.txt, llms.txt, sitemap.xml) go in `web/public/`.

**Tech Stack:** React 19, TanStack Router v1.95, Vite 6, Tailwind 4, pnpm, Python 3

> **Note on working directory:** The main checkout's `web/` has some files deleted from the working tree. Work from an active worktree where the full web app builds (e.g. `.worktrees/html-renderer` or a new feature worktree). The canonical file paths below match the `web/` layout regardless of which worktree you use.

---

## File Map

**New files:**
- `web/public/robots.txt` — static, allow all + sitemap pointer
- `web/public/llms.txt` — static, agent API instructions
- `scripts/build_sitemap.py` — reads `catalog.json` → writes `web/public/sitemap.xml`
- `tests/test_build_sitemap.py` — pytest for the sitemap script

**Modified files:**
- `web/index.html` — base `<title>`, `<meta name="description">`, `<meta name="twitter:card">`, `<meta property="og:site_name">`
- `web/src/routes/index.tsx` — replace dev-note placeholder with real homepage
- `web/src/routes/__root.tsx` or `web/src/components/TopBar.tsx` — update header tagline
- `web/src/routes/ley.$numero.$fecha.tsx` — dynamic head (title/meta/OG/JSON-LD) + mode labels + citation footer
- `web/src/components/ModificationsPanel.tsx` — "Abrir + saltar →" → "Abrir y navegar", "⤴ segundo plano" → "Abrir sin saltar"
- `web/src/components/ChronologyPanel.tsx` — "causa →" → "ley origen →"

---

## Task 1: Static SEO files

**Files:**
- Create: `web/public/robots.txt`
- Create: `web/public/llms.txt`

- [ ] **Step 1.1: Create `web/public/robots.txt`**

```
User-agent: *
Allow: /

Sitemap: https://pisanvs.github.io/ley-chile/sitemap.xml
```

- [ ] **Step 1.2: Create `web/public/llms.txt`**

```markdown
# ley·chile

> El corpus jurídico chileno completo en formato legible para agentes y humanos.
> Todas las leyes, decretos y códigos de Chile desde su promulgación, con historial
> de versiones y diff entre modificaciones. Datos oficiales de la BCN.

## Acceso programático

### Descubrir normas

Buscar por número de ley (e.g. "20720"):
- GET https://pisanvs.github.io/ley-chile/idx/by-numero.json
  → objeto { "20720": 1003455, ... } que mapea número → idNorma

Índice completo de títulos y tipos:
- GET https://pisanvs.github.io/ley-chile/idx/titles.json

### Historial de versiones de una norma

- GET https://pisanvs.github.io/ley-chile/idx/commits/{idNorma}.json
  → { norma: { id_norma, numero, tipo, titulo, organismo, fecha_publicacion },
      commits: [{ sha, date, causa_id, subject, magnitude }],
      rel_dir: "leyes/20720" }

Cada commit representa una publicación legislativa. `date` es YYYY-MM-DD.
`causa_id` es el idNorma de la norma que causó este cambio (puede ser la misma).
`magnitude` indica el tamaño del cambio en líneas.

### Texto de una versión específica

- GET https://raw.githubusercontent.com/pisanvs/ley-chile/{sha}/{rel_dir}/texto.md
  → texto completo en Markdown, artículos delimitados por encabezados

- GET https://raw.githubusercontent.com/pisanvs/ley-chile/{sha}/{rel_dir}/metadata.json
  → metadatos de esa versión (fecha, organismo, título, etc.)

Para obtener el texto vigente (versión más reciente), usa el sha del último commit
en el array `commits` del historial.

### Diff entre versiones

GitHub expone diffs directamente desde los commits de la rama `historial`:

Cambios introducidos por un commit específico (una publicación legislativa):
- GET https://github.com/pisanvs/ley-chile/commit/{sha}.diff  → unified diff patch

Diff entre dos versiones cualesquiera:
- GET https://github.com/pisanvs/ley-chile/compare/{shaA}...{shaB}.diff  → patch completo

Cada commit en `historial` representa exactamente una publicación legislativa,
por lo que `commit/{sha}.diff` equivale a "qué cambió en esta reforma".

### Grafo de modificaciones

Leyes que esta norma modificó:
- GET https://pisanvs.github.io/ley-chile/idx/modifies/{idNorma}.json

Leyes que modificaron a esta norma:
- GET https://pisanvs.github.io/ley-chile/idx/modified_by/{idNorma}.json

### Repositorio git completo

El historial legislativo completo está disponible como repositorio git:
- git clone --branch historial --single-branch https://github.com/pisanvs/ley-chile historial

Cada commit en la rama `historial` corresponde a una publicación legislativa.
El mensaje de commit identifica la norma causante.
Los archivos dentro de cada commit siguen la estructura:
  leyes/{numero}/texto.md
  leyes/{numero}/metadata.json
  modificaciones/{numero}/texto.md  (para leyes modificatorias)
  dl/{numero}/texto.md              (decretos ley)
  dfl/{organismo}/{numero}/texto.md (decretos con fuerza de ley)

## Ejemplos de uso

Obtener texto vigente de la Ley 20.720:
1. GET idx/by-numero.json → idNorma = 1003455
2. GET idx/commits/1003455.json → tomar sha del último commit
3. GET raw.githubusercontent.com/pisanvs/ley-chile/{sha}/leyes/20720/texto.md

Ver qué cambió en una reforma específica:
1. GET idx/commits/{idNorma}.json → identificar el sha del commit de esa reforma
2. GET https://github.com/pisanvs/ley-chile/commit/{sha}.diff → patch completo

Comparar dos versiones arbitrarias:
- GET https://github.com/pisanvs/ley-chile/compare/{shaA}...{shaB}.diff

Identificar qué leyes han modificado el Código del Trabajo:
1. Resolver número → idNorma vía by-numero.json
2. GET idx/modified_by/{idNorma}.json
```

- [ ] **Step 1.3: Commit**

```bash
git add web/public/robots.txt web/public/llms.txt
git commit -m "feat(seo): add robots.txt and llms.txt"
```

---

## Task 2: Base HTML meta tags

**Files:**
- Modify: `web/index.html`

- [ ] **Step 2.1: Replace `web/index.html`**

```html
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ley·chile — El corpus jurídico chileno</title>
    <meta name="description" content="Leyes, decretos y códigos de Chile con diff visual entre versiones, historial completo y comparación en cualquier fecha. Datos oficiales de la BCN. Para humanos y agentes." />
    <meta property="og:site_name" content="ley·chile" />
    <meta name="twitter:card" content="summary" />
    <link rel="preconnect" href="https://raw.githubusercontent.com" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2.2: Verify dev server starts without errors**

```bash
cd web && pnpm dev
```

Open http://localhost:5173 — browser tab should show `ley·chile — El corpus jurídico chileno`.

- [ ] **Step 2.3: Commit**

```bash
git add web/index.html
git commit -m "feat(seo): base title and meta tags in index.html"
```

---

## Task 3: Homepage copy

**Files:**
- Modify: `web/src/routes/index.tsx`

- [ ] **Step 3.1: Replace `web/src/routes/index.tsx`**

```tsx
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/')({
  component: HomePage,
})

function HomePage() {
  return (
    <div className="flex flex-col min-h-full">
      <main className="flex-1 max-w-3xl mx-auto w-full px-6 py-16 md:py-24">
        <h1 className="font-display text-4xl md:text-5xl leading-[1.1] text-balance mb-6">
          El corpus jurídico chileno,{' '}
          <span className="text-indigo">en formato amigable.</span>
        </h1>
        <p className="text-ink-soft text-lg leading-relaxed mb-2 max-w-xl text-balance">
          La BCN publica todas las leyes de Chile. Pero cuando una ley modifica a
          otra, leer el cambio es casi imposible — y volver atrás en el tiempo para
          ver cómo era antes, directamente imposible.
        </p>
        <p className="text-ink-soft text-lg leading-relaxed mb-10 max-w-xl">
          ley·chile lo convierte en algo legible.
        </p>

        <div className="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-16">
          <Feature
            title="Diff visual"
            desc="Ve exactamente qué cambió entre versiones, línea por línea."
          />
          <Feature
            title="Máquina del tiempo"
            desc="¿Qué decía esta ley en 2005? Viaja a cualquier fecha desde su promulgación."
          />
          <Feature
            title="Modificaciones cruzadas"
            desc="Qué leyes modificó esta norma, y quién la modificó a ella."
          />
        </div>

        <div className="rounded-lg border border-rule bg-paper-sunk p-6">
          <p className="text-xs uppercase tracking-widest text-ink-faint mb-2 font-ui">
            Para desarrolladores e investigadores
          </p>
          <p className="text-sm text-ink-soft leading-relaxed mb-4">
            Todo el corpus está disponible como repositorio git — cada publicación
            legislativa es un commit con metadatos completos. Ideal para pipelines,
            análisis y agentes.
          </p>
          <a
            href="https://github.com/pisanvs/ley-chile"
            target="_blank"
            rel="noopener noreferrer"
            className="inline-flex items-center gap-1.5 text-sm text-indigo hover:underline font-ui"
          >
            Ver repositorio en GitHub →
          </a>
        </div>
      </main>

      <footer className="border-t border-rule px-6 py-4 text-[11px] text-ink-faint text-center">
        Datos: Biblioteca del Congreso Nacional (BCN) · Actualización semanal
      </footer>
    </div>
  )
}

function Feature({ title, desc }: { title: string; desc: string }) {
  return (
    <div className="rounded-md border border-rule bg-paper p-4">
      <div className="font-ui text-sm font-medium text-ink mb-1">{title}</div>
      <div className="text-xs text-ink-soft leading-relaxed">{desc}</div>
    </div>
  )
}
```

- [ ] **Step 3.2: Verify homepage**

```bash
cd web && pnpm dev
```

Open http://localhost:5173. Confirm:
- H1: "El corpus jurídico chileno, en formato amigable."
- Three feature cards visible
- Developer callout box below the fold
- Footer: "Datos: Biblioteca del Congreso Nacional (BCN) · Actualización semanal"
- No "Time Machine landing arrives in Plan 4" or "idNorma" text anywhere

- [ ] **Step 3.3: Commit**

```bash
git add web/src/routes/index.tsx
git commit -m "feat(copy): real homepage replaces dev placeholder"
```

---

## Task 4: Per-law dynamic head — title, meta, OG, JSON-LD

**Files:**
- Modify: `web/src/routes/ley.$numero.$fecha.tsx`

React 19 hoists `<title>`, `<meta>`, and `<link>` rendered inside components to `<head>`. Use this directly — no additional library needed. For `<script type="application/ld+json">`, use `useEffect` (React 19 does not hoist script tags).

- [ ] **Step 4.1: Add `useJsonLd` hook to `ley.$numero.$fecha.tsx`**

Add this after the existing import block, before the `Route` declaration:

```tsx
function useJsonLd(json: Record<string, unknown>) {
  useEffect(() => {
    const script = document.createElement('script')
    script.type = 'application/ld+json'
    script.text = JSON.stringify(json)
    document.head.appendChild(script)
    return () => { document.head.removeChild(script) }
  // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [JSON.stringify(json)])
}
```

- [ ] **Step 4.2: Add `LawHead` component to `ley.$numero.$fecha.tsx`**

Add this before the `IDEPage` function. It uses `capitalize` and `truncate` which are already defined at the bottom of the file:

```tsx
function LawHead({
  norma,
  active,
  commitCount,
  idNorma,
}: {
  norma: { tipo: string; numero: string; titulo: string; fechaPublicacion: string }
  active: Commit | undefined
  commitCount: number
  idNorma: number
}) {
  const pageTitle = `${capitalize(norma.tipo)} N° ${norma.numero} — ${truncate(norma.titulo, 60)} | ley·chile`
  const description = `${capitalize(norma.tipo)} N° ${norma.numero} — texto en cualquier fecha desde ${norma.fechaPublicacion}, con diff visual entre versiones. ${commitCount} publicaciones registradas. Datos BCN.`
  const ogTitle = `${capitalize(norma.tipo)} N° ${norma.numero} | ley·chile`
  const url = `https://pisanvs.github.io/ley-chile/ley/${idNorma}/${active?.date ?? ''}`

  useJsonLd({
    '@context': 'https://schema.org',
    '@type': 'Legislation',
    name: `${capitalize(norma.tipo)} N° ${norma.numero}`,
    alternateName: norma.titulo,
    legislationIdentifier: norma.numero,
    datePublished: norma.fechaPublicacion,
    jurisdiction: { '@type': 'AdministrativeArea', name: 'Chile' },
    publisher: {
      '@type': 'Organization',
      name: 'Biblioteca del Congreso Nacional de Chile',
      url: 'https://www.bcn.cl',
    },
    url,
  })

  return (
    <>
      <title>{pageTitle}</title>
      <meta name="description" content={description} />
      <meta property="og:title" content={ogTitle} />
      <meta property="og:description" content="Texto vigente con diff visual e historial. Datos BCN." />
      <meta property="og:type" content="article" />
      <meta property="og:url" content={url} />
      <link rel="canonical" href={url} />
    </>
  )
}
```

- [ ] **Step 4.3: Render `<LawHead>` in `IDEPage`**

In `IDEPage`, find the `return (` at the end of the function (the one that returns `<IDEShell ...>`). Replace it with:

```tsx
  return (
    <>
      <LawHead
        norma={idx.norma}
        active={active}
        commitCount={idx.commits.length}
        idNorma={idx.norma.idNorma}
      />
      <IDEShell
        center={center}
        rightRail={<RightRail idx={idx} active={active} activeSlug={activeSlug} />}
      />
    </>
  )
```

- [ ] **Step 4.4: Verify dynamic head in browser**

```bash
cd web && pnpm dev
```

Navigate to a law page (e.g. `/ley/1003455/2023-01-01`). Check:
- Browser tab title: `Ley N° 20.720 — {titulo truncated} | ley·chile`
- DevTools → Elements → `<head>`: must contain `<meta name="description">`, `<meta property="og:title">`, `<meta property="og:url">`, `<link rel="canonical">`, `<script type="application/ld+json">`
- Navigate back to `/` — title reverts to `ley·chile — El corpus jurídico chileno`

- [ ] **Step 4.5: Commit**

```bash
git add "web/src/routes/ley.\$numero.\$fecha.tsx"
git commit -m "feat(seo): dynamic title, meta, OG and JSON-LD on law pages"
```

---

## Task 5: UI microcopy

### 5a — Mode labels and citation footer (`ley.$numero.$fecha.tsx`)

**Files:**
- Modify: `web/src/routes/ley.$numero.$fecha.tsx`

- [ ] **Step 5a.1: Update mode labels in `ModeToggle`**

Find:
```tsx
  const opts: { id: ReaderViewMode; label: string; needsDiff?: boolean }[] = [
    { id: 'redline', label: 'Redline', needsDiff: true },
    { id: 'side-by-side', label: 'Lado a lado', needsDiff: true },
    { id: 'clean', label: 'Limpio' },
    { id: 'source', label: 'Fuente' },
  ]
```

Replace with:
```tsx
  const opts: { id: ReaderViewMode; label: string; title?: string; needsDiff?: boolean }[] = [
    { id: 'redline', label: 'Tachado', title: 'Vista redline — cambios marcados', needsDiff: true },
    { id: 'side-by-side', label: 'Paralelo', needsDiff: true },
    { id: 'clean', label: 'Texto limpio' },
    { id: 'source', label: 'Markdown' },
  ]
```

Then update the `<button>` inside the map to forward the `title` prop:
```tsx
          <button
            key={o.id}
            disabled={disabled}
            onClick={() => setMode(o.id)}
            title={o.title}
            className={...existing className...}
          >
            {o.label}
          </button>
```

- [ ] **Step 5a.2: Update citation footer in `formatCitation`**

Find:
```tsx
  return `${head}${date}.\nTexto: ${url}\nVía ley·chile (BCN, https://pisanvs.github.io/ley-chile)`
```

Replace with:
```tsx
  return `${head}${date}.\nTexto: ${url}\nley·chile · Biblioteca del Congreso Nacional (BCN)`
```

- [ ] **Step 5a.3: Commit**

```bash
git add "web/src/routes/ley.\$numero.\$fecha.tsx"
git commit -m "fix(copy): Spanish mode labels, spell out BCN in citation"
```

### 5b — ModificationsPanel

**Files:**
- Modify: `web/src/components/ModificationsPanel.tsx`

- [ ] **Step 5b.1: Update "Abrir + saltar →"**

Find:
```tsx
          Abrir + saltar →
```
Replace with:
```tsx
          Abrir y navegar
```

- [ ] **Step 5b.2: Update "⤴ segundo plano"**

Find:
```tsx
          ⤴ segundo plano
```
Replace with:
```tsx
          Abrir sin saltar
```

- [ ] **Step 5b.3: Commit**

```bash
git add web/src/components/ModificationsPanel.tsx
git commit -m "fix(copy): clarify action labels in ModificationsPanel"
```

### 5c — ChronologyPanel

**Files:**
- Modify: `web/src/components/ChronologyPanel.tsx`

- [ ] **Step 5c.1: Update "causa →"**

Find:
```tsx
          causa →
```
Replace with:
```tsx
          ley origen →
```

- [ ] **Step 5c.2: Commit**

```bash
git add web/src/components/ChronologyPanel.tsx
git commit -m "fix(copy): 'causa →' → 'ley origen →' in ChronologyPanel"
```

---

## Task 6: Header tagline

**Files:**
- Modify: `web/src/routes/__root.tsx` or `web/src/components/TopBar.tsx` (whichever contains the tagline)

- [ ] **Step 6.1: Locate the tagline**

```bash
grep -r "corpus jurídico" web/src/
```

This will identify the exact file. The tagline text is `corpus jurídico en vivo`.

- [ ] **Step 6.2: Update the tagline**

In the file found above, find:
```tsx
          corpus jurídico en vivo
```
Replace with:
```tsx
          Para agentes y humanos.
```

(The compact nav header can't fit the full tagline. "Para agentes y humanos." is the memorable, distinctive second line — concise enough to sit beside the wordmark.)

- [ ] **Step 6.3: Verify header**

```bash
cd web && pnpm dev
```

Header should read: `ley·chile  Para agentes y humanos.`

- [ ] **Step 6.4: Commit**

```bash
git add web/src/routes/__root.tsx web/src/components/TopBar.tsx 2>/dev/null; git commit -m "fix(copy): header tagline → 'Para agentes y humanos.'"
```

---

## Task 7: Sitemap generator

**Files:**
- Create: `scripts/build_sitemap.py`
- Create: `tests/test_build_sitemap.py`

`catalog.json` structure: `{ "entries": [{ "idNorma": 1, "tipo": "...", "fechaPublicacion": "..." }, ...] }`

- [ ] **Step 7.1: Write the failing test `tests/test_build_sitemap.py`**

```python
import json
from pathlib import Path
from xml.etree import ElementTree as ET

import pytest

from scripts.build_sitemap import build_sitemap, SITE_BASE


def make_catalog(tmp_path: Path, entries: list) -> Path:
    p = tmp_path / "catalog.json"
    p.write_text(json.dumps({"entries": entries}))
    return p


def test_sitemap_includes_homepage(tmp_path):
    catalog = make_catalog(tmp_path, [])
    out = tmp_path / "sitemap.xml"
    build_sitemap(str(catalog), str(out))
    tree = ET.parse(out)
    locs = [u.find('{http://www.sitemaps.org/schemas/sitemap/0.9}loc').text
            for u in tree.getroot()]
    assert SITE_BASE + "/" in locs


def test_sitemap_includes_norma(tmp_path):
    catalog = make_catalog(tmp_path, [{"idNorma": 12345, "tipo": "ley"}])
    out = tmp_path / "sitemap.xml"
    build_sitemap(str(catalog), str(out))
    tree = ET.parse(out)
    locs = [u.find('{http://www.sitemaps.org/schemas/sitemap/0.9}loc').text
            for u in tree.getroot()]
    assert SITE_BASE + "/ley/12345" in locs


def test_sitemap_empty_entries(tmp_path):
    catalog = make_catalog(tmp_path, [])
    out = tmp_path / "sitemap.xml"
    build_sitemap(str(catalog), str(out))
    tree = ET.parse(out)
    locs = [u.find('{http://www.sitemaps.org/schemas/sitemap/0.9}loc').text
            for u in tree.getroot()]
    assert locs == [SITE_BASE + "/"]


def test_sitemap_valid_xml(tmp_path):
    catalog = make_catalog(tmp_path, [{"idNorma": 1, "tipo": "ley"}])
    out = tmp_path / "sitemap.xml"
    build_sitemap(str(catalog), str(out))
    ET.parse(out)  # raises if invalid


def test_sitemap_skips_entries_without_id(tmp_path):
    catalog = make_catalog(tmp_path, [{"tipo": "ley"}, {"idNorma": 99, "tipo": "dl"}])
    out = tmp_path / "sitemap.xml"
    build_sitemap(str(catalog), str(out))
    tree = ET.parse(out)
    locs = [u.find('{http://www.sitemaps.org/schemas/sitemap/0.9}loc').text
            for u in tree.getroot()]
    assert SITE_BASE + "/ley/99" in locs
    assert len([l for l in locs if "/ley/" in l]) == 1
```

- [ ] **Step 7.2: Run to verify failure**

```bash
python -m pytest tests/test_build_sitemap.py -v
```

Expected: `ModuleNotFoundError: No module named 'scripts.build_sitemap'`

- [ ] **Step 7.3: Create `scripts/build_sitemap.py`**

```python
"""Generate sitemap.xml from catalog.json for ley·chile."""
from __future__ import annotations

import argparse
import json
from pathlib import Path
from xml.etree import ElementTree as ET

SITE_BASE = "https://pisanvs.github.io/ley-chile"
NS = "http://www.sitemaps.org/schemas/sitemap/0.9"


def build_sitemap(catalog_path: str, out_path: str) -> None:
    data = json.loads(Path(catalog_path).read_text())
    entries = data.get("entries", data) if isinstance(data, dict) else data

    ET.register_namespace("", NS)
    root = ET.Element(f"{{{NS}}}urlset")

    def add_url(loc: str, changefreq: str, priority: str) -> None:
        el = ET.SubElement(root, f"{{{NS}}}url")
        ET.SubElement(el, f"{{{NS}}}loc").text = loc
        ET.SubElement(el, f"{{{NS}}}changefreq").text = changefreq
        ET.SubElement(el, f"{{{NS}}}priority").text = priority

    add_url(SITE_BASE + "/", "weekly", "1.0")
    for entry in entries:
        id_norma = entry.get("idNorma") or entry.get("id_norma")
        if id_norma is None:
            continue
        add_url(f"{SITE_BASE}/ley/{id_norma}", "monthly", "0.8")

    tree = ET.ElementTree(root)
    ET.indent(tree, space="  ")
    Path(out_path).parent.mkdir(parents=True, exist_ok=True)
    tree.write(out_path, encoding="unicode", xml_declaration=True)


if __name__ == "__main__":
    parser = argparse.ArgumentParser()
    parser.add_argument("--catalog", default="catalog.json")
    parser.add_argument("--out", default="web/public/sitemap.xml")
    args = parser.parse_args()
    build_sitemap(args.catalog, args.out)
    print(f"Wrote {args.out}")
```

- [ ] **Step 7.4: Run tests to verify all pass**

```bash
python -m pytest tests/test_build_sitemap.py -v
```

Expected: 5 tests pass.

- [ ] **Step 7.5: Smoke-test the script**

```bash
python scripts/build_sitemap.py --catalog catalog.json --out /tmp/sitemap-test.xml
head -10 /tmp/sitemap-test.xml
```

Expected: XML starts with `<?xml ...>` and contains `<loc>https://pisanvs.github.io/ley-chile/</loc>`.

- [ ] **Step 7.6: Commit**

```bash
git add scripts/build_sitemap.py tests/test_build_sitemap.py
git commit -m "feat(seo): sitemap.xml generator from catalog.json"
```

---

## Task 8: CI — add sitemap generation step

**Files:**
- Modify: `.github/workflows/build-pages.yml`

- [ ] **Step 8.1: View the current workflow**

```bash
cat .github/workflows/build-pages.yml
```

- [ ] **Step 8.2: Add sitemap step before `pnpm build`**

In the job that runs `pnpm build`, insert this step immediately before it:

```yaml
      - name: Generate sitemap
        run: python scripts/build_sitemap.py --catalog catalog.json --out web/public/sitemap.xml
```

`catalog.json` is committed to the repo, so it's always available without a prior download step.

- [ ] **Step 8.3: Commit**

```bash
git add .github/workflows/build-pages.yml
git commit -m "ci: generate sitemap.xml before pages build"
```

---

## Self-Review

### Spec coverage

| Spec item | Task |
|---|---|
| `index.html` base `<title>` + `<meta description>` | Task 2 |
| `og:site_name`, `twitter:card` base | Task 2 |
| Homepage — replace placeholder | Task 3 |
| Per-law dynamic `<title>` | Task 4 |
| Per-law `<meta name="description">` | Task 4 |
| Per-law Open Graph tags | Task 4 |
| Per-law JSON-LD `Legislation` structured data | Task 4 |
| `<link rel="canonical">` per law | Task 4 |
| Header tagline → "Para agentes y humanos." | Task 6 |
| Mode labels: Tachado / Paralelo / Texto limpio / Markdown | Task 5a |
| Citation footer: spell out BCN | Task 5a |
| ModificationsPanel: "Abrir y navegar" + "Abrir sin saltar" | Task 5b |
| ChronologyPanel: "ley origen →" | Task 5c |
| `robots.txt` | Task 1 |
| `llms.txt` | Task 1 |
| `build_sitemap.py` | Task 7 |
| CI workflow sitemap step | Task 8 |

All 13 implementation scope items covered. ✓

### Type consistency

- `useJsonLd(json: Record<string, unknown>)` defined and called only in `LawHead` — consistent.
- `LawHead` receives `norma` typed as `{ tipo, numero, titulo, fechaPublicacion }` — matches the `NormaMeta` shape from `CommitsIndex.norma`.
- `capitalize` and `truncate` are used in `LawHead` — both are already defined in `ley.$numero.$fecha.tsx`, no duplication.
- `build_sitemap(catalog_path: str, out_path: str)` and `SITE_BASE` — exported and imported consistently across script and tests.

### No placeholders

No TBD, TODO, or incomplete steps. All code blocks are complete and self-contained. ✓
